# 구현 기록

## 구조 선택

- 원본 기획안 MD/HTML은 수정하지 않았습니다.
- Vite + TypeScript + vanilla DOM/SVG를 사용했습니다. 카드·표·폼·포커스 관리가 중심이어서 전체 Canvas는 사용하지 않았습니다.
- JSON 콘텐츠를 `src/data/content.ts`에서 타입이 보장된 값으로 노출하고, 엔진은 DOM과 LocalStorage를 참조하지 않는 순수 함수로 구성했습니다.
- 난수는 문자열 시드를 FNV-1a로 해시한 뒤 xorshift32로 전개합니다. 주사위와 생활사건 선택이 동일 시드에서 재현됩니다.

## 주요 밸런스 값

값은 `src/data/balance-config.json`과 `src/data/policy-rules.json`에서 조정할 수 있습니다.

| 항목 | 값 | 구현 의도 |
|---|---:|---|
| 시작 IRP | 108,000,000원 | 기본 목표 50만원에 가깝지만 즉시 달성하지 않는 출발점 |
| 시작 생활자금 | 12,000,000원 | 납입과 생활사건 사이의 선택 여지 |
| 턴당 급여 잉여자금 | 1,400,000원 | 꾸준한 납입을 가능하게 하되 사건 비용과 충돌 |
| 기본 추가납입 | 1,000,000원 | 한 행동의 영향이 읽히는 단위 |
| 기본 거래금액 | 8,000,000원 | 상품비중 변화가 화면에서 보이는 단위 |
| 생활자금 안전 기준 | 6,000,000원 | 2별 안정성 조건 |
| 위험자산 교육용 한도 | 70% | 정책 JSON에서만 관리 |
| 수령기간 | 240개월 | 월 연금 단순 균등분할 |
| 예금 교육용 만기 | 3턴 | 그 전 해지 시 1.2% 불이익 |
| 최대 낙폭 3별 기준 | 12% | 성향·분산과 함께 지속가능성 평가 |
| 최소 분산 기준 | 평가액 5% 이상 3종 | 이름만 보유한 소액 상품은 제외 |

초기 포트폴리오는 기획안대로 원리금보장형 60%, 혼합형 40%입니다. 리밸런싱 목표는 예금 30%, 단기채 15%, 장기채 10%, 혼합형 25%, 국내주식 ETF 10%, TDF 10%입니다.

## 주문과 정책 구현 메모

- 펀드/TDF 매수대금과 환매대금은 대기주문 중에도 IRP 총자산에 포함해 자산이 사라져 보이지 않도록 했습니다.
- 펀드/TDF는 제출 턴에 `received`, 정산 후 `priced`, 다음 턴 정산에서 잔고에 반영됩니다. ETF와 예금은 즉시 체결됩니다.
- 시장 상승으로 위험비중이 70%를 넘으면 `marketLimitExceeded` 안내만 켜고 규칙 위반 횟수를 올리지 않습니다. 이후 위험자산 추가매수는 차단됩니다.
- 적격 TDF는 데이터의 `tdf_exception_eligible`를 읽어 정책상 유효 위험비율을 70%로 조정합니다. 실제 상품 적격 여부를 뜻하지 않습니다.
- 생활자금이 부족해도 게임오버하지 않고 생활자금 부족 횟수와 안정성 점수에 반영합니다.

## 자동 시뮬레이션 결과

2026-08-12, 1,000개 시드와 4개 자동전략을 순환 실행한 결과는 **당시 별 공식** 기록이다. 현재 공식(목표 95% 근접=1별, 목표+생활자금=2별, 리밸런싱 목표비중 정렬=3별)과 숫자가 다르다. 당시: 0별 505, 1별 5, 2별 245, 3별 245.

현재 공식과 `steward` 전략을 포함한 시뮬 숫자는 아래 「별 사다리 (2026-08-24)」를 본다.

## 별 사다리 (2026-08-24)

- 0별: 월 연금 < 목표의 95%
- 1별: 목표 95% 이상 100% 미만, 또는 목표 달성 + 생활자금 600만 미만
- 2별: 목표 100% + 생활자금 600만 이상. `ruleBreaches`는 별에 넣지 않음
- 3별: 2별 + 낙폭 12% + 성향별 분산 하한 + 공식 리밸런싱 목표 위험비중 ±10%p
- 칭호: 입문자 / 목표에 가까워진 적립가 / 균형 잡힌 적립가 / 지속 가능한 연금 설계자
- `simulate`는 balanced / passive / contributor / growth / steward 를 순환한다.

최종 회귀검증에서 수치는 시뮬레이션 출력과 다시 대조합니다.

## 브라우저 검증 결과

- 375×812: 타이틀부터 결과까지 완주. 납입, 펀드 매수 대기, ETF 즉시매수, 예금 매도, 교체매매, 리밸런싱 확인.
- 1280×900: Tab/Enter만으로 진단, 목표, 튜토리얼, 12턴, 결과까지 완주.
- 모바일 `documentElement.scrollWidth === innerWidth` 확인.
- 모달 높이 666px / 812px 뷰포트, 내부 스크롤과 하단 시트로 버튼 접근 가능.
- Vite 오류 오버레이 없음, 브라우저 페이지 오류 없음.
- axe-core WCAG 2 A/AA 자동 검사: 위반 0건. 배경 그라디언트로 자동 판정할 수 없는 대비 항목은 incomplete로 남으며 직접 시각 확인함.
- 새로고침 후 동작 줄이기 설정, 도감 11개, 최고기록 98점 복원 확인.
- 동일 시드 `17eeys3-wx1xyn` 재시작 첫 주사위 4, 5번 생활사건 도착이 엔진 예측과 일치.

브라우저 캡처는 `artifacts/browser/`에 보관했습니다.

## v0.3 P0 (2026-08-16)

- 한 턴은 시장 브리핑 → (생활사건) → 운용 1회 → 정산이다. 주사위 게이트는 없다.
- 생활사건은 시드당 3회, 학습 카드는 차단 모달이 아니라 해금·한 줄 팁이다.
- 시작 대비 수익률과 납입 제외 운용수익률을 점수에 포함한다.
- 시장 시나리오는 같은 스토리를 유지하되 진폭을 키우고 6·8턴을 충격 턴으로 둔다.
- 면책 동의는 최초 1회만 LocalStorage에 남긴다.
- 1,000시드 시뮬 기준선: 오류 0, 평균 IRP 125,846,367원, 평균 수익률 16.52%, 목표 달성 75.0%, 평균 최대 낙폭 1.95%.

## 알려진 비차단 제한사항

- 실제 사용자 5~8명과 금융·세무 전문가 검수는 소프트웨어 자동검증으로 대체할 수 없으므로 출시 전 별도로 필요합니다.
- 자동 시뮬레이션의 전략은 균형·관망·납입 중심·성장 중심·납입 후 리밸런싱(steward)의 결정 규칙이며 실제 사용자 행동 분포를 뜻하지 않습니다.
- 12턴 진행 트랙의 칸은 작은 화면에서 숫자만 보이고, 현재 국면 이름은 시장 카드에서 제공합니다.
- LocalStorage는 브라우저 설정이나 저장공간 정책에 따라 차단될 수 있으며, 이 경우 게임은 진행되지만 기록은 세션 밖에 남지 않습니다.
